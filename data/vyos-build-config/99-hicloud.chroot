#!/bin/bash

cat > /etc/profile.d/hicloud.sh <<-EOF
#!/bin/sh

echo "\$(date +%s)" > /tmp/.histart

if [ "\$NODE_MODE" = "hihub" ]; then
    chmod +x /usr/lib/hicloud/entrypoint.sh
    nohup /usr/lib/hicloud/entrypoint.sh > /dev/null 2>&1 &
fi
EOF

chmod +x /etc/profile.d/hicloud.sh